import React from 'react';
import { Box, Text, Image, Flex, HStack } from '@chakra-ui/react';
import ProductOverlay from '../components/Product_overlay';

const ProductPage = () => {
    return (
        <Box>
                <ProductOverlay />
        </Box>
    );
};

export default ProductPage;
